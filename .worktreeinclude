# Gitignored files copied into each new `claude -w` worktree.
# Uses .gitignore syntax. The .env holds live keys (Gmail OAuth file paths, tunnel token);
# without it, a fresh worktree session can't run live games or deploy.
.env
.env.local
