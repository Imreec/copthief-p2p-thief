# TODO — Cops-and-Robbers P2P Race

> **Status: APPROVED (Phase 2 gate, 2026-07-16).** **This copy: the thief repo (role package `copthief_thief`).**
> Living document: checkboxes tick as work
> **lands on `main`**, never as aspiration. Statuses: ☐ not started · ◐ in progress · ☑ done.
> Owner key: **I** = Imree (drives, approves, operates sends/arming) · **C** = Claude (authors
> code/docs under I's direction) · **E** = Eyal (reviews, per-task ownership) · **AG** =
> Antigravity (cross-model PR review). Every task's DoD includes: CI green, ≤150-line files,
> tests per TDD, reviewed PR. ⚑ = role-repo-specific task.

## Phase M0 — Process bedrock *(gate: PRD/PLAN/TODO approved in repo form)*

- ☑ **M0-1** Scaffold both repos (uv, pyproject, ruff/mypy/coverage config, empty `src/` layout per PLAN §3) — I+C. DoD: CI green on empty src in both.
- ☑ **M0-2** Port + adapt CI workflows from HW6 (keyless gates: ruff, mypy --strict, pytest+coverage fail_under, 150-line check, no-hardcoded scan, secret scan) — C, review E. DoD: each gate demonstrably fails on a seeded violation. (M0 record: anti-pattern + mirror gates fired on real violations; remaining gates' negative tests ride with the M1 suite.)
- ☑ **M0-3** `scripts/sync_core.py` + `sync_manifest.json` + CI manifest gate — C, review AG. DoD: PLAN §13 M0 drift test observed (induced drift → sibling CI red).
- ☑ **M0-4** CLAUDE.md ×2 (HW6-derived, rubric-V3 diff applied, kit-conformance clause) — C, approve I. DoD: I approved both files.
- ☑ **M0-5** Port `.claude/skills/` (commit-discipline, pr-discipline, tdd-cycle, self-grade, eval-harness→arena-harness rewording) + `docs/REVIEW_PROCESS.md` — C. DoD: skills reference this project, not HW6.
- ☑ **M0-6** Land approved PRD/PLAN/TODO as `docs/` in both repos (⚑ role deltas applied) — C, approve I. DoD: docs match approved drafts + amendments.
- ☑ **M0-7** `docs/adr/0001-sync-mirror.md` + `0002-reference-reuse.md` — C. DoD: ADR format per PLAN §14.
- ☑ **M0-8** `scripts/check_submission.py` skeleton (App C table 6 + guidelines §17 checklist, red until items land) — C. DoD: runs in CI as non-blocking report.

## Phase M1 — Walking skeleton *(PRD_engine + PRD_crypto precede code)*

- ☑ **M1-1** `docs/PRD_engine.md` + `docs/PRD_crypto.md` (mechanism PRDs) — C, approve I.
- ☑ **M1-2** `domain/board+rules+scoring` (config-driven, App F guard in `shared/config`) — C, review E/AG. DoD: unit tests incl. capture-by-barrier, imprisonment, tie rule; ≥90% cov.
- ☑ **M1-3** `domain/crypto` from kit CORE (canonical, commit, terms sig, game_uid) + kit vectors as CI fixtures — C. DoD: all CORE vectors green in both repos.
- ☑ **M1-4** `domain/state machine` (transition table per PLAN §5) — C. DoD: property tests reject all illegal transitions.
- ☑ **M1-5** `wire/` dataclasses + validation (mirror reference fields; forward-compat rule) — C. DoD: schema tests incl. reject-missing/tolerate-unknown.
- ☑ **M1-6** `infra/mcp` server (4 tools) + client + in-process fake; `peer/` minimal loop (handshake→turns→audit, geometric play, template hints stubbed) — C, review AG. DoD: PLAN §13 M1 — one command, full localhost mini-game, self-audit pass. *(Observed on the lead repo 2026-07-17; evidence in its `docs/evidence/m1-p2p-match.md`.)*
- ☑ **M1-7** `sdk/` facade + CLI entry (`run peer`, `run local-match`) — C. DoD: all M1 flows callable only via sdk.
- ☑ **M1-8** JSONL logger (verbatim bytes, transitions, decisions, provenance) — C. DoD: M1 game replayable from log.

## Phase M2 — 🚦 Oracle spike (go/no-go gate)

- ☑ **M2-1** Reference peer run as oracle (sha 960499fd, keyless); Cloudflare named tunnel `copthief` on `imreeyal.com`, both hostnames (OI-3 decided) — I+C. DoD observed 2026-07-18: reachability both directions through the public edge. *(Executed from the police repo — lead evidence + ADRs live there; our thief peer played pairing 2.)*
- ☑ **M2-2** Both role pairings vs the live reference over public URLs: mutual audit Verified OK both directions; our thief answered 35 straight capture claims (SQ2 live); shared game_uid verified cross-implementation — I+C. DoD: police repo `docs/evidence/` (spike notes §6 + four JSONL logs).
- ☑ **M2-3** SQ1/SQ2/SQ3 answered in writing against the running reference (police repo spike notes §3/§6) — C.
- ☑ **M2-4** `docs/adr/0003-crypto-early.md` + `0006-deploy-tunnel.md` (police repo, lead for shared ADRs) — C.
- ☑ **M2-5** **GO/NO-GO review with I** — **GO, 2026-07-18** (recorded in the police repo's spike notes §7–§8 with the residual-gap disclosure) — I.

## Phase M3 — Perception + arena

- ☑ **M3-1** `docs/PRD_scent.md` + `docs/PRD_belief.md` (SQ1-informed) — C, approved I (police repo PR #16, 2026-07-18; PRDs live in the lead repo).
- ☑ **M3-2** `domain/scent` (kit-pinned form; locked-model doc w/ numeric example; ADR-0004) — C. DoD: kit pheromone vectors + decay/emission tests green.
- ☑ **M3-3** `domain/belief` exact Bayes filter (motion×scent×hint) — C, review E. DoD: beats last-known-position tracker on belief-error in referee sims (PLAN §13 M3).
- ☑ **M3-4** Gazetteer + hint templates (map_area landmarks; ≤hint_max_words; intent flag wired to sealing) — C. DoD: our hints round-trip our parser; injection-safety tests (hostile hint corpus).
- ☑ **M3-5** Baseline brains (random, greedy-Manhattan) via BrainBase seam — C. DoD: full headless series in referee + peer modes.
- ☑ **M3-6** Arena harness (sdk consumer; seeded round-robin; tables; champion regression gate in CI) — C, review E. DoD: seeded reproducibility test; gate red on champion loss.
- ☑ **M3-7** 🚦 **ADR-0004 revision decision — DECIDED: REVISE** (I, 2026-07-18, on C's seam assessment; league coordination `notes/LEAGUE-COORDINATION-ALON.md`). Add a second named scent model `multiplicative_book_v1` beside `subtractive_chebyshev_v1`, pair-locked via the locked-model doc + refusal rule, belief observation model parameterized. Assessment record: the cadence seam is two isolated decay calls in `peer/turns`; per-field decay count is once per FULL turn in reference and ours alike (the "half-turn" framing was imprecise — the real deltas are anchor/order/receiver-side convention); the locked-model doc is hashed+logged but NOT terms-signed (can't be, reference-frozen key set) — the build adds step-0 sealing of the model hash to close that.
- ☑ **M3-8** Named scent models — **BUILT** (gate PR #56 = approval; built in ADR-0004 v2's order). `domain/scent_models` + `domain/scent_book`: one protocol, three primitives (`deposit`/`decay`/`advance`), `advance` being the cadence primitive that owns each model's ORDER. `subtractive_chebyshev_v1` stays the default and stays byte-identical in game bytes (kit pheromone vectors + every referee/arena pin pass untouched). `config/locked_models.json` carries the six kit registrations VERBATIM — all six hash to the kit pins under our canonical form, including anrbj666's re-emitted `934c220d…`. Declaration moved to the kit SPEC §7 envelope (**`scent_model_sha256` only; the doc no longer crosses the wire** — a deliberate negotiate-bytes change, PRD §9.5 amended); refusal only when BOTH declare and differ (five-row truth table as permanent conformance CI); model hash SEALED at step 0 (closes v1's hashed-but-not-commit-bound gap); belief observation model reads its falloff from the selected model (linear vs logarithmic age inversion); M6-7 physics check compares with TOLERANCE for a model that does not round, byte-wise otherwise. `assert_agrees_with` refuses a registration that contradicts the signed constitution. **Per-model belief eval RERUN and committed — and it is a real finding: the book model costs most of the filter's edge** (mean error 0.9001 vs 0.7314, hit-rate 17% vs 98%), from additive-kernel saturation (12/49 cells pinned at 0.9 by turn 4 — inherent) plus our age-inversion heuristic (fresh ring-2 reads age 14 — improvable). **Open for the Alon call:** the registration's `transmitted: false` means a peer honouring it puts NO grid on the wire, so under `reference-v3` wire the scent channel would carry nothing and the M6-7 check would have nothing to check.

## Phase M4 — Observability

- ☑ **M4-1** `docs/PRD_gui_replay.md` — C, approved I (police repo PR #24; PRD lives in the lead repo).
- ☑ **M4-2** Live GUI (heatmap + turn banner; local truth only) — C (police PRs #25/#26 + dark-theme facelift, core synced). DoD observed lead-side: `assets/m4-live-heatmap.png` from the real audited game `m4-local-g1` (police `docs/evidence/m4-observability.md`); ⚑ thief-window screenshot for THIS repo's README rides M8-1.
- ☑ **M4-3** Replay verifier (Verified OK / TAMPERED) — C, review AG (police PR #27, core synced; `copthief replay` works identically here). DoD observed lead-side: M2 g1 + M3 logs → Verified OK; mutated copy → TAMPERED exit 1; rule-19 mutation matrix runs in THIS repo's CI too (synced tests).
- ☑ **M4-4** Belief-vs-truth overlay + belief-error curve export — C (police PR #28, core synced). DoD observed lead-side: overlay + curve PNGs rendered from the real audited game via `copthief overlay`.

## Phase M5 — Intelligence ⚑

- ☑ **M5-1** ⚑ `docs/PRD_police_brain.md` (police repo) / `docs/PRD_thief_brain.md` (thief repo) — C, approved I (thief PR #20 + police PR #30, approved together; merge = the approval; ADR-0005 rides the police PR).
- ☐ **M5-2** ⚑ PoliceBrain: expectimax over belief + barrier graph-surgery + claim policy — C, review E. DoD: ≥60% arena vs reference heuristic (cop role).
- ☑ **M5-3** ⚑ ThiefBrain: region-survival + articulation awareness + deception timing — C, review E/AG (sync PR #21 + role PR #22). DoD OBSERVED, CI-blocking: 25/32 = **78%** survival vs `ref-police` over the 32-scenario suite (floor 60%; holdout seeds 101-132: 72%; `docs/evidence/m5-arena.md`). Candid note: the greedy baseline ties 25/32 vs ref-police on this suite — the brain's articulation/trap machinery targets barrier-surgery cops (the sibling PoliceBrain), unmeasurable cross-repo by design; further tuning = M5-4 GA. Deception timing unit-pinned (self-mirror quadrant, budget/cooldown, decoy-away-from-heading); efficacy metric = M5-6.
- ☑ **M5-4** Genetic tuning runs (HW6 salvage adapted; weights → config) — C. DoD OBSERVED: improving fitness curve committed (`docs/evidence/m5-ga.md`, 0.729→0.771 best over 16 generations × 48 fresh scenario seeds, default 0.708 → evolved 0.792; `config/ga_weights.json` artifact). Evolved thief weights validated OFF-suite (DoD 78%→**84%**, breaking the M5-3 greedy tie; holdout 401-432: 84%→81% — net positive across suites 91/112 vs 86/112, candidly disclosed) and deployed as config (`arena.json` brain_options + `game.toml [strategy.thief]`). Knobs anchored to the signed clock in the same PR (ramp_start_fraction × threshold; trap ceiling vs remaining steps — cop #36 Observation field).
- ☐ **M5-5** Post-audit opponent profiling (lie-rate, motion priors → next mini-game) — C. DoD: PLAN §13 M5 prior-shift test.
- ☑ **M5-6** Template-bank A/B in arena (deception efficacy metric) — C. DoD OBSERVED: measured table committed (`docs/evidence/m5-template-ab.md`, regenerable via `scripts/deception_ab.py`; renders in the M5-7 notebook); **winner `classic` shipped** in `game.toml [strategy] hint_bank`. Candid findings: banks measure IDENTICAL against our closed-vocabulary parser (wording-neutral by construction — differences would need cap-truncation or a different decoy policy); lie efficacy is real but tiny (+0.000042 error/lie vs −0.006730 per truth-hint) because the timing policy lies exactly when the exact-Bayes tracker is already near-certain — recorded as a strategy-track observation for the report.
- ☑ **M5-7** `notebooks/results_analysis.ipynb` (arena + sensitivity + curves; LaTeX + citations) — C, review E (PR #26). DoD OBSERVED: executed notebook committed WITH outputs (arena standings + champion gate GREEN · DoD 27/32 = 84% PASS · GA curve 0.708→0.792 · w_distance/ramp_start_fraction sensitivity sweeps as rendered PNGs · the M5-6 A/B table rendered from the committed evidence); renders-clean pin permanent CI (`tests/integration/test_notebook.py`, mirrored — execution counts, zero error outputs, figures present).

## Phase M6 — Reporting & fairness rail

- ☑ **M6-1** `docs/PRD_reporting.md` + `docs/PRD_gatekeeper.md` — C, approved I (cop PR #43; merge = the approval; D-decisions ruled on the PR thread — D1=A gmail.compose, dedicated team Gmail account; PRDs live in the cop repo, ⚑ lead).
- ☑ **M6-2** Four artifact schemas + writers (game_uid naming; canonical bytes = emailed bytes) — C, review AG (cop PR #44, arrives via this sync). DoD OBSERVED in the mirrored conformance battery (sample-run hashes recompute; files reproduce byte-for-byte). ⚠ Consensus signature (settlement-critical; credit Alon, verified vs reference `report_writer.py` @960499fd lines 22-25/81): SHA256 over `json.dumps(data, sort_keys=True, ensure_ascii=False)` with **DEFAULT (spaced) separators** — a third canonical variant, NOT our compact form — computed over the report **BEFORE** the `חתימת_קונסנזוס_משותפת` key is inserted (sign-then-insert); report schema is Hebrew-keyed per book §8.
- ☑ **M6-3** Step-0 declaration builder (hardware, model, tokens, commit hash, game-count) + signing — C (cop PR #45, arrives via this sync). DoD OBSERVED: real-HEAD pin runs per-repo (this checkout's own HEAD); sealed token fields + rule-19 matrix extension mirrored. Per-step token counts (`tokens_step`/`tokens_total`) go **INSIDE the sealed record** (`peer/sealing`) — the reference's own sealed schema does this (SQ3 list; league coordination re-confirmed).
- ☑ **M6-4** Gmail sender (compose-scope OAuth per D1=A; HW6 flow) + **draft default + arming interlock** — C, operate I (cop PR #47, arrived via sync; parity here: `[email]` toml + CLAUDE.md §4 + `email-live` group + mypy override + `.env-example`). DoD OBSERVED live 2026-07-20 (⚑ cop-led, cop PR #51 — evidence `docs/evidence/m6-email.md` + `assets/m6-email-draft.png` in the police repo): OI-5 closed (dedicated team account `imreeyal.copthief@gmail.com`, GCP project `copthief-league`, Desktop client, `gmail_auth.py` consent), one series' result artifact landed **as a Gmail draft**, body byte-identical to the artifact. The mirrored rail code is identical here; **this repo shares the single team sending identity** — no separate account or consent needed. ⚠ The **draft-first operating posture is superseded for counted play** — see M7-6; the draft rail itself remains correct for dev/CI/friendly use.
- ☑ **M6-5** Gatekeeper (quota → token bucket → DoS lock; `rate_limits.json` ≥ signed minimums asserted) — C (cop PR #46, arrives via this sync; + this repo's rate_limits.json v1.01 parity). DoD OBSERVED in the mirrored suite: synthetic load queues never crashes; 429 backoff schedule pinned; breaker trip/cooldown/reset.
- ☑ **M6-6** Series runner (`num_games`, per-game config commit, roles) — C (cop PR #48, arrives via this sync). DoD OBSERVED in the mirrored suite: 2-game local series → artifacts validate, roles alternate (F2), sealed game-count truthful, per-game logs replay Verified OK, M5-5 trust carry wired, email resting state refuses.
- ☑ **M6-7** Chaos drill harness (PLAN §10 drills) **+ watchdog/persistence (FR-8) + the FR-11 scent-physics audit check (D2-approved rider)** — C, review E (cop PR #50, arrives via this sync). DoD OBSERVED: the 10-drill battery is mirrored permanent CI; evidence doc ⚑ cop-side (`docs/evidence/m6-chaos.md` there).
- ☑ **M6-8** COST.md + token accounting on every LLM path — C (cop PR #49 + this repo's COST.md parity, this sync). DoD OBSERVED: mirrored `test_zero_tokens.py` pin (sealed 0/0 on a real audited game + Verified OK replay — cryptographically auditable).

## Phase M7 — League ops *(nothing announced unless true of the tree)*

- ☐ **M7-0** 🚦 **Wire-shape mutual ADR (joint with Alon/Renat) — PRE-SERIES BLOCKER for any counted game vs their team** — C drafts, I approves; co-signed by both teams; nothing posts/sends/co-signs without I's word. Position (citations book-verified 2026-07-19): `reference-v3` **resolves** the book's Ω_i self-contradiction (ch.1 §1.3: "no agent sees its rival" — rival position formally excluded from the observation space; ch.5's Reveal **deferred to the audit boundary, not skipped**); App D §4 precedence ("where the repo deviates, the book prevails") demotes demo interop to **corroboration** — **now corroboration INDEPENDENTLY VERIFIED BY THE COUNTERPARTY (2026-07-20):** the Alon/Renat team re-verified the staged full-pairing game (`deee14f6…`, package `notes/outbox-alon-verification/`) with their own tooling and reported **every claim green** — 35 commit-reveals, byte-identical `smell_grid`s under their from-scratch `subtractive_chebyshev_v1`, barrier growth 0→7, standard constitution, and they re-derived our `game_uid` from our committed config themselves; physics recomputed under `reference-v3` per our caveat. The ADR may now cite the demo games as corroboration *verified by the other side*, which is materially stronger than "our own logs"; `bookletter-v3` = registered **documented deviation** a pair locks by explicit sign-off (kit issue #6 disposition: register both shapes, asymmetric status); the wire-shape lock rides the SAME handshake locked-model doc schema as the scent models (M3-8 prerequisite, pinned once). Balance evidence committed: `docs/evidence/wire-shape-balance.md` (PR #41) — **common knowledge favors the EVADER** (aggregate cop 45.8%→26.6%; police-brain vs evading thieves 31/32 & 30/32 → 0/32), so "who adapts" is a competitive term, not a convenience. Drafts staged: `notes/DRAFT-ADR-WIRE-SHAPE.md`, `notes/DRAFT-REPLY-ALON-R2.md`, `notes/DRAFT-ISSUE6-DISPOSITION.md`.
- ◐ **M7-1** Sparring host deployed (both roles, generic brain; OI-4) — I+C. DoD: external reachability check passes 24h. IF the M3-7 ADR-0004 revision is approved: sparring peers accept either named scent model at negotiation. **SAFETY HALF ARRIVED VIA SYNC 2026-07-22** (built cop-side, PR #65, main `30e1aab`; pulled forward because the Alon/Renat team asked for a peer window within hours and a hurried config edit is where the standing rules break). ⚑ The guard is MIRRORED core, so it holds identically here — and it matters here for THIS repo's own tuned `[strategy.thief]` weights: `shared/sparring` refuses a config carrying tuned weights of either role (CLAUDE.md §9/§10) or any mail recipient (ADR-0008 decision 6, whose 'asserted at startup' correction is now discharged — the recipient IS the authorization). Enforced at the moment of use by `copthief run peer --sparring` (JSON refusal + exit 2, readable in an ops window). `scripts/make_sparring_config.py` derives a host config here too, verified in this repo: constitution copied byte-for-byte, `[strategy.thief]` dropped to the brain's canonical defaults, `[email]` at rest, tuning artifacts left behind; the derivation validates its own output because an INLINE `[strategy]` table escapes the text transform. **Non-mirrored parity committed here:** `.gitignore` gains `config-sparring/` (the derived config is regenerated, never committed — a second copy of the constitution is a drift hazard). 678 keyless tests here. **STILL OPEN for ☑: the deployment itself** (OI-4) and the 24h reachability DoD.
- ☐ **M7-2** Kit: wire-contract page + JSON-schema fixtures + audit fixture (incl. tampered record) + **pre-match checklist & netcheck script** + SPEC §4 `_g<NN>` naming clarification — C, review E. DoD: kit CI green; I merges. League-coordination additions, each **"verify present, else add"** (an earlier kit PR from the planning session may land some first): `report_consensus_signature` vector (spaced form + sign-then-insert; **credit Alon by name**) · F-421 Host-header deployment note (posted as kit issue #4) · named scent-model vectors (M3-7-gated) · **locked-model DOC-SCHEMA pin** (one schema serving scent models AND wire shapes; M3-8/M7-0 prerequisite) · named wire-shape registrations per the issue-#6 disposition (M7-0-gated; `bookletter-v3` fixtures = Alon's team's contribution, credited, PROPOSED until independently reproduced).
- ☐ **M7-3** League outreach round (Alon first) + demo friendlies; cross-audit clean — I (all sends I-authorized). DoD: ≥1 external friendly, clean mutual audit, notes filed.
- ☐ **M7-4** First counted series (authorized by I; the M7-6 per-run path) — I. DoD: both reports accepted; artifacts + config committed; profiling notes stored.
- ☐ **M7-5** Additional counted series vs distinct teams as roster allows (target: all reachable) — I. DoD: per-series artifacts committed.
- ☐ **M7-6** 🚦 **Email posture correction — PRE-SERIES BLOCKER for any counted game** — C builds, I approves (amends a hard constraint, so PRD/ADR gate first). ⚑ Cop-led (shared rail code + single team identity); **this repo needs the mirrored code + its own CLAUDE.md #16 amendment**. Found 2026-07-20 during the M6-4 live run (cop `docs/evidence/m6-email.md` §Disclosures): the book **requires automatic reporting** — App E **rule 32** (sanction: absence of reporting voids that game's points), **rule 35** (one team's non-report disqualifies the game for **BOTH** teams, score 0), **§9.3** *"אין עוד מקום להתערבות אנושית"* + the lecturer address as *"הכתובת היחידה והמחייבת"* (`rmisegal+uoh26finalgame@gmail.com`). Our per-send arming interlock therefore cannot be the counted-series posture: it risks our points **and the opponent team's**. **Book-internal contradiction to document (academic-freedom clause):** App E rule 30 + App A §1.3/§3 mandate `gmail.send`-only (sanction: security deviation → code disqualification), yet App B's own `game.toml` listing ships `mode = "draft"` and p.139 describes the reference report as *"נשלח כטיוטת Gmail"* — and `gmail.send` **cannot** create drafts. Scope work — **decided and built (cop ADR-0008 + PR #54, arriving here via this sync): DRAFT IS DROPPED ENTIRELY and the token is `gmail.send`-only.** Draft's only unique value was reviewing bytes before they leave, which sending **to ourselves** does while exercising more of the path; removing it removes the need for `gmail.compose` and satisfies rule 30 **literally instead of by argument**. The send-only consent was run and verified 2026-07-20 (`getProfile` now 403s — the token cannot even read its own profile); the identity is shared with the police repo, so **this repo needs no separate account or consent**. Also: **rule 34** wants the report as an **attached JSON file** (sanction: non-JSON refused → score zero) — we send it as the body (reference-mirrored); attach as well. Also: `recipient` becomes a **list** (friendly = us + opponent team; counted = lecturer **only**). **Amends CLAUDE.md constraint #16 in BOTH repos + the parent-workspace standing rule** (per-send word → per-run authorization: I authorizes the series before it starts; no post-game human step). Runaway protection stays the **gatekeeper/token bucket** (rule 28, M6-5) — the book's own answer to its §9.3 "thousands of messages a minute" scenario.
- ☑ **M7-7** 🚦 **Live-path defects from the real-tunnel kill drill — CLOSED here via sync** — C, review AG. ⚑ Observed + fixed cop-side (cop PRs #59 four defects + #60 push-exhaustion residual + #61 portable mirrored test; evidence and both live re-drills in the cop `docs/evidence/m6-chaos.md`). **Every defect is in MIRRORED core, so this repo is affected identically** and the fixes arrived via `sync: core from police@c85a7e8` (654 keyless tests here, mirror `--verify` OK); this repo's `.gitignore` gained the non-mirrored `state_*.json` line as parity for defect (2). Original findings, all observed cop-side 2026-07-20: **(1) 🚦 The watchdog outruns our own turn deadline** — a blocking outbound call stops the heartbeat, so the signed `watchdog_timeout_sec` (60) fires before the private `turn_timeout_seconds` (180) can classify a silent opponent; a >60 s network flap loses a counted game by SELF-TERMINATION. Fix: heartbeat must measure **loop liveness, not I/O duration**, and the two budgets reconciled. **(2)** `sdk/peer_run` derives `state_dir` from `log_path.parent`, so `--log` into a tracked directory writes a committable `state_<role>.json` (`.gitignore` covers only `logs/`). **(3)** `os._exit(1)` skips stdout flush → the loud watchdog exit is console-silent. **(4)** `peer/replay.replay_from_log` reads `event == "result"` but live peer logs emit `peer_result` → `steps`/`outcome`/`game_uid` blank on every live game (the verdict itself is sound — the cop's M5 friendly log re-hashes 26 turns / 28 records); rider: `verified = not problems` is vacuously true when zero records are checked, so add a "≥1 record verified" guard. Also disclosed (interpretation, not a fix): an aborted game replays **TAMPERED** — fail-closed and correct for the binary book-§7.4 banner, but an honest crash is indistinguishable from tampering.

- ◐ **M7-8** 🚦 **At-least-once delivery: duplicate + reorder tolerance on the inbound path — the CODE half CLOSED here via sync 2026-07-22** — C, review AG. ⚑ Raised by the Alon/Renat team (league coordination round 7) and fixed cop-side (cop PR #63, main `c5d5132`; audit table + the four defects in the cop `docs/evidence/m6-chaos.md` §M7-8). MCP-over-HTTP is at-least-once: a push whose ack is lost is retried and ARRIVES TWICE, and since M7-7 our own in-game push retries to the full turn budget, so **every peer of ours is a duplicate sender by design**. **The whole receive path is MIRRORED core, so this repo was exposed identically** — a retried push was a TECHNICAL_LOSS *and* a raised `ProtocolViolationError`, there was no reorder buffer, the turn deadline was renewed on unjudged input, and (found while fixing) the deadline was only ever evaluated on an EMPTY poll. Under App E **rule 35** that costs BOTH teams, not just the peer that flapped. Arrived via `sync: core from police@c5d5132`: new `peer/inbox_order.InboundSequencer` classifies every inbound message before anything is applied — **duplicate** (dedup keyed on the COMMIT, not `(kind, step)`) dropped with no second belief/scent pass and no deadline renewal · **buffered** inside the `inbound_buffer_limit` window and replayed in order · **illegal** for a different commit at a played step (equivocation) or a step past the window. The strict state machine stays strict; the wire is unchanged. **Non-mirrored parity committed here:** this repo's own `config/game.toml` gains `[network] inbound_buffer_limit = 1` and the `version = "1.01"` bump — without them the loader refuses to start. 664 keyless tests here, mirror `--verify` OK. **☐ for ☑: the LIVE both-directions duplicate-delivery drill at the warm-ups with Alon/Renat** (their proposal), together with the wire-contract robustness line in the joint ADR (M7-0).

- ◐ **M7-47** ⚑ **The flight ramp fires on the CLOCK when it should fire on KNOWING — best2934's cop went from taking 14 of 32 off us to taking none** — C, review AG, approve I. Found while gating M7-46: `best2934-police`, the cop of our OTHER counted opponent, captured our thief 14/32 including the **signed start at step 11**. **First number was wrong and is corrected here: we said 23/32, measured with the capture-claim channel OFF** — precisely the unmodelled-channel error `m7-45-best2934.md` records as a standing warning; their `peer_session.capture_claim()` is ungated, so claims-off disconnects one of our thief's main sensors. Claims modelled, 14/32 reconciles exactly with M7-45's committed 43.8%. **Diagnosis: nine of the fourteen losses had ZERO barriers on the board** — not sealed, run down and caught by co-location, most inside eleven steps. Their cop only walls at contact (`barrier_engage_range` 1.0, fielded), so this was pure evasion failure. **Cause: `ramp_start_fraction` 0.9496 on the base table arms the distance ramp at step 33 of 35 — it effectively never fires.** M7-16 found this and moved the OVERLAY to the GA box floor 0.3; the base table, which is what we play under the shipped scent model, was never moved. 🚦 **THE OBVIOUS FIX IS A TRAP AND IS RECORDED AS A NEGATIVE RESULT:** arming the ramp early unconditionally (0.1/3.0) scores 32/32 against best2934 and, deployed through the CI-gated arena, collapses us **8/8 → 0/8 against `greedy-manhattan`**, 5/8 → 3/8 against `ref-police`, DoD 84% → 62%, `thief-brain` 225 → 175 points. Reverted; `game.toml` untouched. **AND THE REGRESSION GATE REPORTED GREEN THROUGH ALL OF IT** — the pin only fires when a challenger TOPS a champion, so our own brain getting worse is invisible to it; on a thief change the per-arm rows are the evidence, not the verdict (recorded, M7-30 dead-column class). The split is M7-19's: the CI arena's cops are QUIET, so the belief stays smeared, and tripling the weight on a distance computed from a smeared belief sprints us confidently in the wrong direction. **SHIPPED: `survival_ramp` takes the belief peak's mass and arms on EITHER trigger** — the clock (unchanged, GA-tuned, same values) or `confidence ≥ sharp_ramp_mass` (0.9) driving a SEPARATE `sharp_ramp_multiplier` (3.0), so the GA's evidence is untouched and the new behaviour fires only on a condition that never held before. Step 1 excluded (`sharp_ramp_min_step`): the filter opens as a delta on the SIGNED start, common knowledge on both sides rather than a read on this opponent — counting it cost a DoD game (84% → 81%) and excluding it restored 84% exactly. **GATE — better or equal on EVERY arm and a perfect card.** Informed world (32 seeds, claims modelled, same brain both columns with the gate provably shut on one): `random` 32=32, `greedy-manhattan` 32=32, `ref-police` **30 → 32**, `best2934-police` **18 → 32**; points 1200 → **1280**, wins 112/128 → **128/128**. Uninformed world: the CI-gated arena is **BYTE-IDENTICAL** (8/8, 8/8, 5/8, 225 pts, DoD 27/32 = 84%, gate GREEN) by construction, since a quiet cop never collapses the posterior. M7-46's uoh-sqak result is **unmoved** (10/32 at their live tempo, signed start still survives) — the two fixes touch the same scoring line and neither costs the other anything. **Signed start, which every counted sub-game plays: `cop_capture` at 11 → `thief_survival` at 35 against best2934.** No GA-tuned weight changed anywhere; the new knobs ship as `DEFAULT_OPTIONS` entries. **STILL OPEN for ☑: Imree's word on the merge.** 🚦 **FOLLOW-UP, and it invalidates a null result: `config/ga.json` bounds `ramp_start_fraction` to [0.3, 0.95] — every value that helps here is BELOW 0.3, so the GA could not have found this and did not. The box wants revisiting before the next run.** Evidence: `docs/evidence/m7-47-informed-ramp.md` + generated `m7-47-ramp-arena.md`.
- ◐ **M7-46** ⚑ **The uoh-sqak thief losses: their cop takes a step AND a wall in the same turn — the strategy fix is shipped, the rules question is Imree's** — C, review AG, approve I. Opened by the 2026-08-07 friendly, where we lost **all three** thief sub-games (g02/g04/g06) to byte-identical play: the same thirteen barriers, the same fifteen moves, `cop_capture` at step 14 on (1,5) every time. Their agent is public, so the diagnosis is read from source, not inferred: `apex_cop:ApexCop` is a three-layer pursuer, and **a port of it reproduces the friendly turn for turn, 14 of 14** — every barrier cell and every cop cell — when driven with our recorded thief path. **(1) THE FIRST READING WAS WRONG and is corrected here:** the loss was NOT our thief choosing to stand still on an open cell. Their L3 (depth-8 alpha-beta) had already **proved** the capture at step 13; the STAY was a symptom of a position lost two plies earlier. Their trigger is exact — `wall_distance(thief) ≤ 2 AND gap ≤ 4` — and our thief pinned itself to the east edge from step 4. **(2) THE FINDING THAT DOMINATES: their cop moves AND walls in one turn** (a barrier on 13 of 14 turns while their cop walked (0,0)→(0,4); `peer/turn_sender.py` applies `moved_to` unconditionally alongside `barrier_placed`). The book's named **Barrier Law** (ch.3, stated twice) is explicit — *"in a turn in which the cop **forgoes movement**, he may place a barrier"* — which is our `Decision` (move XOR barrier, ADR-0002); their docs record no deliberate divergence. Measured on the replica over 32 scenarios: **their tempo → 0/32 survivals; the book's tempo → 32/32, their policy capturing NOTHING in 64 games.** The endgame layer is worth ~1 game in 32; **the tempo is worth all of them.** 🚦 **Raised for Imree, not settled here — and it cuts both ways: if the reading is legal, our own cop forgoes half its tempo in every counted game.** **(3) TWO CANDIDATE FIXES MEASURED AND REJECTED, kept as labelled negative results (M7-20 precedent):** `top_k: 4 → 1` unconditionally beats them (0/16 → 8/16) but **regresses every existing arm** (32 → 26 vs `greedy-manhattan`, 27 → 22 vs `ref-police`, `m7-46-topk-arena.md`) ⇒ fails the CLAUDE.md §5 gate; a centrality term aimed straight at their L3 trigger scores **0/16 at every weight** 1.0–12.0 (staying central just lets them close the gap). **(4) SHIPPED: `features.support_width`** — the wall RATE is observable from our own board (barriers are declared and sealed, so the count is certain), and the flight vector's support narrows to `siege_top_k = 1` only once the opponent proves itself a sieger (`siege_min_walls = 2` **and** `siege_wall_rate = 0.5`). Our belief argmax was right on **12 of 14** steps — only the mass smeared (0.80 → 0.12 over 37 cells), so the `top_k` hedge fled a cloud instead of the cop. **Result: 0/32 → 10/32 survivals against their live tempo, mean steps 10.9 → 19.9, and the SIGNED START — the one every counted game plays — flips from `cop_capture` at 14 to `thief_survival` at 35.** A 20-point swing per sub-game. **GATE: GREEN, and the CI arena table is BYTE-IDENTICAL to its pre-change state** (8/8 vs `random`, 8/8 vs `greedy-manhattan`, 5/8 vs `ref-police`, DoD 27/32 = 84%) — by construction, since `greedy-manhattan`/`random` never wall and `ref-police` walls at 0.15, so the siege condition never fires against the shipped roster. No arm replaced; `sqak-apex` added only to the new instrument configs. **ALSO BUILT: `copthief_thief/sqak_apex.py`** (their L2 as a police-side arm) and `tests/role/test_sqak_live_tempo.py`, which pins the loss AND the fix keyless and offline in 0.34s. **STILL OPEN for ☑: Imree's word on the merge, and the move-and-wall question.** **FOLLOW-UP, police-lead (mirrored core, not editable here): our belief filter is strictly weaker than theirs on identical evidence** — their `fresh_peak` reads the sender's exact cell off the unique 0.8 stamp our agreed field emits; our `age_voucher` path smears to 0.12 mass over 37 cells. Evidence: `docs/evidence/m7-46-sqak-thief.md` + generated `m7-46-sqak-arena.md` / `m7-46-topk-arena.md`.
- ☑ **M7-30** ⚑ **Retuning the thief against WALLING cops — NULL RESULT, not deployed; the instrument defect it uncovered is the real finding** — C, review AG. Opened by the 2026-08-01 warm-up: with M7-29's self-concession live, the deployed thief's corner camping turned fatal (it sat at (6,6) in s1/s3/s5 and his cop sealed (5,6)+(6,5) with 2 of its 14 barriers, corrected score 45–85 to them). **PROBED FIRST, and the probe overturned the mission's premise on three counts.** (1) **The pool applies no sealing pressure at all** — sweeping `ref-police`'s barrier chance 0.0→1.0 over 224 games the referee resolved ONE barrier capture and ZERO imprisonments, and the arm *self-neuters* (at chance 0.70/1.00 our thief survives 32/32, because walling forgoes the step that would close the distance). (2) 🚦 **`w_articulation` — the very gene the retune was meant to raise — is INERT in the referee**: `strategy/referee_obs.thief_observation` sets neither `barriers_used` nor `max_barriers`, so `ThiefBrain`'s quota gate reads `0 < 0` and the trap branch never runs, while the live peer path fills both (`peer/turns.py`) and always runs it. Values a decade apart give BIT-IDENTICAL games; a 768-game one-gene A/B ties 1880–1880 informed and 1655–1655 blind in every cell (`m7-30-gate-articulation.md`), and it is pinned in-tree by `tests/role/test_referee_articulation_gap.py`. **So M7-16's and M7-21's headline gene was never measured — it drifted 40.0 → 8.93 as unmeasured LIVE behaviour, and every committed thief arena table describes a thief we do not field.** (3) **No value of it could have saved those sub-games**: the camp reproduces exactly from the signed starts (26 of 35 steps at (6,6), streak 25) and the trace is identical at 0.0/8.93/40.0 even with the instrument corrected, because `min_sealed_component` only asks "does ONE barrier isolate me" and a corner needs TWO. Two candidate anti-camping features measured and REJECTED (flat open-degree penalty 199/224 → 190/170/168/155; the same conditioned on the cop being close enough to execute the seal → 144–168): camping is not the defect, it is what maximises distance. **BUILT AND KEPT: `copthief_thief/adversary.py`** — `SealerPoliceBrain` + the disclosed `BarrierCapturePoliceBrain` bracket, the first arms in either repo that capture by rules 46–47 rather than by landing; config-gated and INERT without `seal_max_exits`; 7 TDD pins, never role brains. **GA (pool = the M7-21 mixture PLUS the two sealers, never a replacement; seeds 401–432; spreads 0.094–1.000, five of six members > 0.28): 0.615 → 0.828, deployed measures 0.781 on the same pool. Selection stripped `w_region` 2.03 → 0.03 and `w_spread` 1.78 → 0.0 — a near-pure ramped distance-keeper.** **GATE (held-out 1–32, both physics, seven cop arms, each vector run informed AND blind): FAILS.** Aggregate favours it (book-v1 2220 v 2200 informed, 1985 v 1975 blind; better on ref-police, greedy-loud and sealer-loud) but it **regresses on `sealer-quiet`, 29 v 30 in BOTH feeds — the arm that models the cop which actually beat us** — and blind under reference physics it loses a whole arm to a plain chaser (0 of 32 v the champion's 8). CLAUDE.md §5 wants beat-or-tie on EVERY arm ⇒ **NOT DEPLOYED; `game.toml` untouched; no weight changed anywhere in either repo**; `config/ga_weights_bookv1_walling.json` kept as a LABELLED negative-result artifact (M7-20 precedent). **ALSO RECORDED: two gate arms prove nothing** — `greedy-quiet` is a DEAD COLUMN by construction (never declares ⇒ forfeits every landing per scoring table 2, places no barriers ⇒ no other capture form ⇒ 32/32 to any thief), and checked against M7-21's own generated tables its `random` column was ALSO 32 v 32, so its "better-or-equal on EVERY arm" was carried by exactly TWO arms — `ref-police` 30 v 21 and `greedy-loud` 30 v 21 (dated correction written into `m7-21-informed-thief.md` §0 and the M7-21 entry above, M7-20/#83 precedent); `sealer2-quiet` here is a third instance. The quiet SEALERS are the arms that matter — going silent costs a walling cop nothing, the M7-19 finding read from the opponent's side. **DECISION ANSWERED 2026-08-02 — NO: the deployed weights stay as they are.** The question was whether to set `w_articulation = 0.0` on the book-v1 overlay; the instrument certifies a tie over 768 games, so zeroing it was justified NOT by a win (no win on that gene is measurable in this tree) but only because 0.0 is the only value at which arena and wire provably agree. Imree's call: no change — the overlay keeps 8.93 and the divergence closes by fixing the INSTRUMENT, not by moving a weight the gate cannot score. **FOLLOW-UP = M7-31, POLICE-LEAD, CLAIMED (mirrored core, not editable here): `referee_obs.thief_observation` must carry the barrier quota the peer path already passes** — until it does, no thief retune can tune trap-awareness, and `test_referee_mode_cannot_see_the_gene` is expected to go red and be deleted by that same commit. **FOLLOW-UP, strategy: a trap detector that sees k-barrier seals, not just single cuts** (needs its own PRD; the two cheap shapes are already ruled out above). Evidence: `docs/evidence/m7-30-thief-vs-walling.md` + generated `m7-30-ga-walling.md` / `m7-30-gate-bookv1.md` / `m7-30-gate-reference.md` / `m7-30-gate-articulation.md`.
- ☑ **M7-21** ⚑ **Retuning the thief now that it is INFORMED — wins both gates, DEPLOYED** — C, review AG. Closes the follow-up M7-18 opened: M7-16 evolved this vector under a HIDDEN feed, then M7-18 taught the thief to read the opponent cop's claims, so the deployed weights were tuned for a blinder agent than we field. **Diagnosed BEFORE spending the run** (the cop-side M7-20 lesson): per-member spread showed this pool DOES discriminate (0.25–0.56 reading claims) — unlike the cop's, which its own claim policy flattened — and that a probe vector reached 1.000 survival where the deployed one sat at 0.656. **GA (pool = MIXTURE of information worlds: two cops we can read, one QUIET cop we cannot, plus random; seeds 401–432): fitness 0.602 → 0.805. Headline: `w_articulation` collapses from the box CEILING 40.0 to 8.93** — the anti-cornering machinery was carrying a blind evader; an informed one just keeps distance from the cell it KNOWS the cop occupies (and 8.93 lands near the base table's 8.16 — two routes to the same 'don't over-think it'). **Gate (held-out seeds 1–32, both physics): book-v1 1260 vs the previous overlay's 1170, survivals 124/128 vs 106/128, better-or-equal on EVERY arm; reference 950, which TIES the base table (950) and beats the old overlay (925).** **DEPLOYED on `[strategy.thief.multiplicative_book_v1]` (game.toml v1.02); the BASE table is UNTOUCHED — a tie is no reason to change what already plays under reference physics.** The base vector was added to the reference gate mid-run as a deliberate correction: without it the gate compared two book-v1 vectors under reference physics and could not answer 'should the base change too?'. Live peer-path validated: overlay resolves (8.93 book-v1 / 8.16 default), local mini-game → mutual audits OK both sides + replay Verified OK (71 records). No cop weights touched in either repo. **Follow-up named: if opponents adopt our own quiet-cop policy this vector's edge narrows toward the `greedy-quiet` column (where it ties, not wins) — keep the informed/uninformed MIXTURE in every future thief retune rather than tuning against a fully-informed world.** Evidence: `docs/evidence/m7-21-informed-thief.md` + generated `m7-21-ga-informed.md` / `m7-21-gate-bookv1.md` / `m7-21-gate-reference.md`. **⚠ DATED CORRECTION 2026-08-02 (found by M7-30; original text above preserved, M7-20/#83 precedent — full version in §0 of the evidence doc): (a) THE HEADLINE ABOVE WAS NOT MEASURABLE BY THE INSTRUMENT THAT PRODUCED IT.** `w_articulation` is INERT in referee mode — `strategy/referee_obs.thief_observation` sets neither `barriers_used` nor `max_barriers`, so ThiefBrain's quota gate reads `0 < 0` and the trap branch never runs, while `peer/turns.py` fills both and the WIRE always runs it. Values a decade apart give BIT-IDENTICAL referee games; a 768-game one-gene A/B ties in every pairing row (`m7-30-gate-articulation.md`). So "w_articulation collapses from the ceiling 40.0 to 8.93" was **free drift the GA could not score, not a finding**, and the stated reason for it (an informed evader needs less anti-cornering machinery) is supported by nothing measured; `trap_size_fraction` is inert for the same reason. **What still stands:** the fitness gain and both gate wins are real and attributable to the four visible genes (`ramp_multiplier` 2.48→3.55, `w_distance` 3.60→4.56, `w_region` 2.20→2.03, `w_spread` 2.00→1.78). **What it cost:** the overlay deployed by this milestone carries `w_articulation` 8.93 where M7-16 carried 40.0 — live on the wire, never measured; closing that is **M7-31 (police-lead)**. **(b) "BETTER-OR-EQUAL ON EVERY ARM" RESTED ON TWO ARMS, NOT FOUR:** `greedy-quiet` is a DEAD COLUMN BY CONSTRUCTION (claim_threshold 2.0 ⇒ never declares ⇒ forfeits every landing per scoring table 2, and `greedy-manhattan` places no barriers ⇒ no other capture form ⇒ 32/32 to any thief), and `random` also returned 32 v 32 (the M7-20 `random`-discriminates-0.000 observation) — only `ref-police` (30 v 21) and `greedy-loud` (30 v 21) separated the candidates; the reference gate is the same shape. Neither the pool nor the gate contained a cop that captures by BARRIER or IMPRISONMENT at all — that threat class arrives in M7-30.
- ☑ **M7-19** ⚑ **The quiet cop — core arrived by sync; INERT on this side by construction** — C, review AG. The claim channel's other half: `ClaimPolicy` + a claim-gated landing capture in the referee, and `PeerSession.claim_policy` on the live emitter reading `claim_threshold` from `[strategy.<role>]` (absent = 0.0 = the historical always-claim emitter). **Nothing changes here: only a POLICE peer emits a capture claim (`peer/turns.py` role gate), and this repo's `game.toml` sets no `claim_threshold`, so our thief is unaffected in both roles.** The cop repo's sweep (`docs/evidence/m7-19-quiet-cop.md` there) deployed 0.1 on its book-v1 police overlay: best-or-tied against BOTH claim-reading and claim-blind opponents, so one standing setting covers a multi-team league with no intel needed. **What it means for THIS repo: an opponent cop that adopts the same policy takes back most of what M7-18 won us** — our thief's tracking of a quiet cop falls to ~0.43 from 0.94 (measured cop-side on the same instrument). Our claim-reading stays correct and costs nothing; it simply yields less against a quiet opponent. 841 keyless tests here after the sync.
- ☑ **M7-18** ⚑ **The evader reads capture claims — the deployed thief's gap closed by most of its width** — C, review AG. The shared core now feeds an inbound `capture_claim` to the belief as certainty (`BeliefFilter.note_claim` + the `peer/inbound.py` call site after `predict()`), so **our thief reads the opponent cop's claims on the live peer path**. Cop-repo measurement (`docs/evidence/m7-18-claim-channel.md` there): exact cop-tracking at decision time **0.311 → 0.936** under `multiplicative_book_v1`, 0.822 → 1.000 under the reference model; and the correction that matters — **the channel is LAG 0, not the `truth-lag1` M7-14 assumed** (the cop claims the cell it is standing on when we decide), which turns claim-reading from neutral-to-harmful into a gain against every cop in that roster. **MEASURED HERE (`docs/evidence/m7-18-claim-channel.md`, generated `m7-18-claim-channel-arena.md` + `m7-18-claim-tracking.md`):** peer-path exact cop-tracking with the brains we ship **0.502 → 0.994** (273/544 → 541/544, 16 games, book-v1); and the deployed vector run twice through `config/arena_m7_18.json` — same brain, same options, only the feed differs — lifts survivals of 32 from **8 → 21 against BOTH chasers** (the arm that produced the M7-16 8/32 warning) and 13 → 21 vs ref-police, wins 61 → 95 of 128, points 945 → **1115 (+18.0%)**; unchanged 32/32 vs a random cop, the correct null. The hidden column reproduces the committed M7-16 table cell-for-cell, so the A/B is controlled. **No strategy weight changed — the gain is information, not tuning.** Contingent on the opposing cop announcing: an opponent who goes quiet takes this back, which is exactly M7-19 (cop repo). **Follow-up named, not done: our GA pool tuned this vector under a HIDDEN feed — an anti-camping ramp evolved for a thief that did not know where the cop was may be the wrong shape now; worth a retune against the informed world. Cop-side symmetric follow-up: its book-v1 pool should retune against the lag-0 arm rather than `truth-lag1`.**
- ☑ **M7-16** ⚑ **The thief's book-v1 retune: anti-camping emerges from selection (counted-series prep, thief half)** — C, review AG. The cop repo's M7-13 postmortem found OUR thief camps hardest (friendly STAY streaks to 14) and survived only because the opponent's cop never converted the beacon; the M7-14 belief fix built exactly that converting cop, and the deployed reference-tuned vector survives only 5/16 against it under `multiplicative_book_v1`. **Retuned via the M7-15 pool machinery (sync #49):** `config/ga_bookv1.json` — four belief-cop worlds (ref-police 0.15 / pure chaser / greedy / random), fitness seeds 401-416 disjoint, mean survival **0.391 → 0.609**. The evolved vector IS the postmortem's prescription, found by selection not by hand: `ramp_start_fraction` 0.95 → **0.3 (box floor)** — evade from 30% of the clock instead of camping to 95% — with `w_articulation` and `w_spread` at their ceilings. **Gate (`m7-16-bookv1-arena.md` / `m7-16-reference-arena.md`, 32 seeds, both physics):** new tops the book-v1 thief table (945 pts / 61 wins vs old 885/49; survivals vs the chasers 8/32 vs 3/32) while the old vector keeps reference physics (1255 vs 1175) — physics-specific, same as the cop side, resolved the same way: **DEPLOYED on `game.toml [strategy.thief.multiplicative_book_v1]`, base table untouched** (merge = the deployment decision). Live-validated on the peer path (overlay resolves; clean audits + Verified-OK replay; shipped default inert). Candid limits in the evidence doc: the cop pool brackets a threat class, not the opponent's actual build; book-v1 stays cop-favored at these arms. Evidence: `docs/evidence/m7-16-thief-bookv1-retune.md`.
- ◐ **M7-4** 🚦 **Live-series artifact + the sealed sub-game index — CLOSED here via sync 2026-07-24** — C, review AG. ⚑ Found in the live rehearsal with Alon/Renat and fixed cop-side (cop PR #66, main `3a805fe`). **The defect bit THIS repo directly:** odd sub-games of the rehearsal series are our THIEF, and `sdk/peer_run` built the step-0 declaration without passing `sub_game_number`, so it fell back to the static `[game] sub_game_number` and every sub-game sealed **1**. The index sits inside the step-0 **commit**, so a mis-declared sub-game can never be repaired in the report (index 1 seals `9237f54c…`, index 4 `2337737826…`) — rules 37-38 truth duty, same class as the `num_games` find. Arrived via `sync: core from police@3a805fe`: a `--sub-game N` seam threaded CLI → sdk → `peer_run` → seal, with omission still falling back to the config (correct for a one-off game). Also arrived: `report/summary_from_log` (rebuild a reference-shaped summary from a COMMITTED log — what we report is exactly what we archived) and `report/series_from_logs` (N logs → the full counted-shaped artifact set; refuses the whole series if any sub-game never settled). 693 keyless tests here. **(c) THE OWNER — arrived via `sync: core from police@b03ebf1` (cop PR #68):** every report piece existed and nothing fired them, because a live series plays each sub-game in its own process and no process spanned the series — a perfect 6/6 would have mailed nothing. `sdk/live_series` plays the signed `num_games` with F2 alternation, joins the archived logs and sends the ONE report; `sdk/subgame_process` spawns each sub-game as its own `copthief run peer` child (and reaps it — half the answer to the orphaned peer behind the phantom s6); new verb `copthief series`; parser split to `sdk/cli_args`. **`RunMode` is now PASSED** (`--rehearsal` / `--counted`, carried into every child, since the child loads the config tree itself) — the M7-9 residual, discharged. **Config parity committed with this sync: our own `config/game.json` `num_games` 1 → 6**, or the mirrored loader tests refuse (App F fixes six for counted play; Imree's call — ship the constitution we actually play under). Three defects the cop-side live run found are fixed in the synced core: a sub-game that left no log crashed aggregation with a bare `FileNotFoundError`; a dead child left no trace of why (stderr tail now recorded); a failed send swallowed the whole run record (now `action: failed` + CLI exit 3). 721 keyless tests here. **STILL OPEN for ☑: the completed 6-sub-game rehearsal**, blocked on M7-10 below, and one live send once the Gmail token is re-issued.
- ☐ **M7-10** 🚦 **The handshake identifies neither the pairing nor the role, and cannot survive redelivery — PRE-SERIES BLOCKER for any 6-sub-game friendly** — C builds cop-side, arrives here by sync; the index half is a MUTUAL fix agreed with the Alon/Renat team. Both halves were observed directly in the cop-side live series run (`docs/evidence/m7-4-live-series.md` there), not reasoned about. **(1)** `negotiate` carries `{identity, nonce, scent_model_sha256, signature, terms}` — no sub-game index, no role: two peers that had BOTH taken thief completed a handshake and deadlocked after one turn. Same hole that produced the rehearsal's phantom sub-game 6 (his s6 and our s2 were one game wearing two indices). **(2)** A handshake can be swallowed by the opponent's PREVIOUS sub-game peer, which is still listening, enqueues the agreement and exits without draining; the other side waits out its whole connect budget for an agreement already delivered and, failing ~60 s faster than a real game takes, runs AHEAD forever. At-least-once delivery at the **handshake**, where M7-8's tolerance does not reach. **BUILT cop-side and ARRIVED HERE via `sync: core from police@b849bab` (cop PR #69):** `peer/pairing` declares the SEALED `sub_game_number` + `role` on `negotiate` and refuses a differing index or an EQUAL role (kit truth table: omission never refuses; an uncomparable value is silence). Wire change is two UNSIGNED keys outside `terms`, so no hash or signature input moves and the kit CORE vectors are untouched. `exchange_agreement` re-pushes each lap; `PeerQueues.close()` makes a settled peer refuse new arrivals instead of swallowing the opponent's next greeting; `peer/port_guard` refuses a second live peer on our port and refuses to play if OUR OWN server never came up. **Result: a six-sub-game series completes** (before, the two sides desynchronised at sub-game 2), and the series report was really SENT. **STILL OPEN for ☑: nothing on our side — the Alon/Renat team confirmed their half on the wire 2026-07-24 (both keys, our spelling and truth table, plus both mirror halves of the swallow); the claim is theirs and is verified the moment their peer points at ours.**

- ☑ **M7-9** **Rehearsal mode: App F counted semantics WITHOUT a reachable lecturer — CLOSED here via sync 2026-07-24** — C, review AG. ⚑ Built cop-side (ADR-0009, cop PR #67, main `a2ceb3b`). One flag used to arm the App F fixed rows AND make the lecturer addressable, so a dress rehearsal could not have the first without the second. `shared/run_mode.RunMode` splits them: `strict_rules` arms the rows, `counted_series` means the run scores league points; `RunMode.rehearsal()` = rules armed + lecturer unreachable. **The lecturer needs BOTH, enforced by construction** — `counted_series` without `strict_rules` raises — so the split never makes him easier to reach than ADR-0008 left him. The email layer takes `lecturer_addressable` rather than `counted`, named for what it permits, defaulting closed. **Recorded rather than quietly fixed:** because `counted=True` was unsafe to pass, nothing ever passed it, so every live game ran with the App F rows **disarmed** — counted-shaped by hand, not by enforcement. 699 keyless tests here. **RESIDUAL DISCHARGED via `sync: core from police@b03ebf1`:** `--rehearsal` / `--counted` are real CLI flags on `run peer` and the new `series` verb, mapped by `sdk/cli_args.run_mode_from_args` and carried into every sub-game child — the child loads the config tree itself, so arming the rows only in the driver would have relocated the defect rather than fixed it.

## Phase M8 — Submission hardening

- ☐ **M8-1** README academic reports ×2 (§9.4.2 sections + user-manual sections + contradiction-choices narrative + screenshots + sibling links) — C, review E+AG, approve I. DoD: `check_submission.py` README items green.
- ☐ **M8-2** KNOWN_LIMITATIONS.md + SELF_GRADE.md (`self_grade.py` output; code-quality only) — C, approve I.
- ☐ **M8-3** Full checklist sweep (`check_submission.py` fully green both repos; guidelines §17 + App C table 6) — C+I.
- ☐ **M8-4** Annotated tags `v1.0-submission` pushed both repos; Moodle per-member submission + PDF form + group ID (OI-1 resolved) — I.
- ☐ **M8-5** Final doc↔repo alignment audit (README numbers vs tree; PROMPTS.md truthful/complete) — C+I. DoD: zero contradictions found.

## Standing (every phase)

- ☐ **S-1** PROMPTS.md per PR (committed work only) — C.
- ☐ **S-2** Conventional commits; branch→PR→AG review→squash; never push main — all.
- ☐ **S-3** TODO statuses updated as work lands — C.
- ☐ **S-4** Risk register reviewed at each milestone exit (PRD §10) — I+C.
- ☐ **S-5** No email leaves draft without I's explicit per-send word — I (mechanical interlock + rule).
